[rule]
id = "no-eval-usage"
description = "eval() should not be used due to security and maintainability concerns"
severity = "error"

[match]
language = "python"
query = """
(call
  function: (identifier) @func
  (#eq? @func "eval")) @violation
"""
